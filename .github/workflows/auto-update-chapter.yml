name: Auto Update Chapter Every Day

on:
    schedule:
        - cron: '0 0 * * *'

jobs:
    ping:
        runs-on: ubuntu-latest
        steps:
            - name: Ping the render service to update all stories
              run: curl -s "${{ secrets.RENDER_SERVICE_URL }}/update-all-story" > /dev/null
